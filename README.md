# Homebrew Tap for PhotonShell

To install PhotonShell via Homebrew:

```bash
# Tap the repository (requires access)
brew tap stennkool/photonshell

# Install PhotonShell
brew install --cask photonshell
```

## Maintenance
This repository is automatically updated by the release workflow in the main PhotonShell repository.
